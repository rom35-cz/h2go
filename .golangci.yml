linters:
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - revive

run:
  timeout: 5m
  tests: false

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0